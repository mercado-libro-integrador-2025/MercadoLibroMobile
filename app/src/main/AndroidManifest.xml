<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ispc.mercadolibromobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_logo_splash"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="false"
        android:theme="@style/Base.Theme.MercadoLibroMobile"
        tools:replace="android:supportsRtl"
        tools:targetApi="31">
        <activity
            android:name="com.ispc.mercadolibromobile.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ispc.mercadolibromobile.activities.LoginActivity"
            android:theme="@style/Base.Theme.MercadoLibroMobile" />

        <activity
            android:name="com.ispc.mercadolibromobile.activities.MainActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.MercadoLibroMobile">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="checkout"
                    android:scheme="mercadolibromobile" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ispc.mercadolibromobile.activities.Politicas"
            android:theme="@style/Base.Theme.MercadoLibroMobile" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>